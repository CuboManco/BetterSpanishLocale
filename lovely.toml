[manifest]
version = "1.0"
dump_lua = true
priority = 0

[[patches]]
[patches.regex]
target = "functions/UI_definitions.lua"
pattern = "text = localize\\('k_round'\\), scale = 0\\.42"
position = "at"
payload = "text = localize('k_upper_round'), scale = 0.42"
match_indent = true

# TODO: Doesn't work
# [[patches]]
# [patches.regex]
# target = "functions/misc_functions.lua"
# pattern = 'gsub\\("\\(%d%d%d\\)", "%1,"\\):gsub\\(",\\$", ""\\)'
# position = "at"
# payload = 'gsub("(%d%d%d)", "%1 "):gsub(" $", "")'
# match_indent = true

# TODO: Also doesn't work
# [[patches]]
# [patches.pattern]
# target = "functions/misc_functions.lua"
# pattern = 'gsub("(%d%d%d)", "%1,"):gsub(",'
# position = "at"
# payload = 'gsub("(%d%d%d)", "%1 "):gsub(" '
# match_indent = true
